<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RPGest</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="#">RPGest</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#">Veiculos</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-5">
        <div class="row">
            <div class="col-lg-8">
                <% if(veiculos.length > 0) { %>
                    <% for (let i = 0; i < veiculos.length; i++) { %>
                        <div class="card mb-4">
                            <img src="<%= veiculos[i].url_imagem %>" class="card-img-top" alt="Veiculo imagem">
                            <div class="card-body">
                                <h2 class="card-title"><%= veiculos[i].marca %> / <%= veiculos[i].placa %></h2>
                                <p class="card-text"><%= veiculos[i].descricao %></p>
                            </div>
                        </div>
                    <% } %>
                <% } else { %>
                    <p>Nenhum veiculo cadastrado.</p>
                <% } %>
            </div>
            <% if(veiculos.length > 0) { %>
            <div class="col-lg-4">
                <div class="card my-4">
                    <h5 class="card-header">Veiculos</h5>
                    <div class="card-body">
                        <ul>
                            <% for (let i = 0; i < veiculos.length; i++) { %>
                                <li><%= veiculos[i].marca %>/<%= veiculos[i].placa %></li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>
    <footer class="text-center py-3">
        <p>&copy;RPGest@2023</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
